"use server"

// This file is created to satisfy Next.js build requirements
// All actual admin operations use API routes for security
// No server actions are currently defined here as they are handled via API routes

export async function placeholder() {
  return { success: true }
}
